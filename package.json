{
  "name": "lp-service-site",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "resend": "^2.0.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
